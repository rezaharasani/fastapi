services:
  fastapi:
    build:
      target: dev
    env_file:
      - .env.dev
    environment:
      - ENVIRONMENT=development
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
  db:
    env_file:
      - .env.dev
